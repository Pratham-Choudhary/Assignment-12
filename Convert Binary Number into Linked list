class Solution {
public:
    int getDecimalValue(ListNode* head) 
    {
        int r = 0;
        while (head != NULL) 
        {
            r = r << 1;
            r += head->val;
            head = head->next;
        }
        return r;
    }
};
