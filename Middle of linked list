class Solution {
public:
    ListNode* middleNode(ListNode* head)
    {
        ListNode *a, *b;
        a=head;
        b=head;
        while(a->next)
        {
            a=a->next->next;
            b=b->next;
        }
     return b;
    }
};
